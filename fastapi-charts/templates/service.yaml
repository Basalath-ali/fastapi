apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-charts.fullname" . }}
  labels:
    {{- include "fastapi-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-charts.selectorLabels" . | nindent 4 }}
